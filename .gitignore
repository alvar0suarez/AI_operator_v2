__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example

# Build del sitio. Se regenera; no se versiona.
sitio/build/
# sitio/docs/ lo ensambla scripts/construir-sitio.py a partir de contenido/.
# Versionarlo duplicaría cada nodo y se desincronizaría al primer cambio.
sitio/docs/
site/
.cache/

# Dependencias del tutor
tutor/serverless/node_modules/

# NO se ignora dataset/SOLUCIONES/: tiene que estar versionado para que la
# corrección objetiva de ESPECIFICACION.md §5.3 funcione. Lo que se hace es
# excluirlo del build público del sitio, no del repositorio.

# Directorios de trabajo temporales
**/_auditoria/
*.tmp
